# Bold math test — delete after checking

Which of these render **R** in bold? Compare each against the plain `$R_0$` at the end of its line.

1. mathbf braced: $\mathbf{R}_0$ vs plain $R_0$
2. mathbf unbraced: $\mathbf R_0$ vs plain $R_0$
3. boldsymbol: $\boldsymbol{R}_0$ vs plain $R_0$
4. pmb: $\pmb{R}_0$ vs plain $R_0$
5. vec arrow: $\vec{R}_0$ vs plain $R_0$
6. bm: $\bm{R}_0$ vs plain $R_0$

Display-math versions:

$$\text{1. } \mathbf{R}_0 \qquad \text{3. } \boldsymbol{R}_0 \qquad \text{4. } \pmb{R}_0 \qquad \text{5. } \vec{R}_0 \qquad \text{plain } R_0$$
